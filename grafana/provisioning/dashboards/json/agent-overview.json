{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prom" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list" }, "tooltip": { "mode": "single" } },
      "targets": [
        {
          "expr": "rate(agent_jobs_completed_total[5m])",
          "legendFormat": "jobs/s",
          "refId": "A"
        }
      ],
      "title": "Agent jobs completed (rate)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prom" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "histogram_quantile(0.99, sum(rate(agent_job_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p99 duration",
          "refId": "A"
        }
      ],
      "title": "Job duration P99 (s)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prom" },
      "gridPos": { "h": 6, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        { "expr": "sum by (agent) (rate(agent_steps_total[5m]))", "legendFormat": "{{agent}}", "refId": "A" }
      ],
      "title": "Agent steps by role",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["agents"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timezone": "browser",
  "title": "Multi-Agent Platform",
  "uid": "multi-agent",
  "version": 1
}
